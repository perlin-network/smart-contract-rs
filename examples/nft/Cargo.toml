[package]
name = "nft"
version = "0.1.0"
authors = ["Abby <abbychau@gmail.com>"]
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
smart-contract = { path = "../../smart-contract" }
smart-contract-macros = { path = "../../smart-contract-macros"}
